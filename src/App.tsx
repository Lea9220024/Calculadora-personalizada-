import React, { useState, useEffect } from 'react';
import { Category, FinancialCard, InstallmentPlan, MonthlyBudget, NetWorthSnapshot, PatrimonyItem, Subscription, FutureCommitment, Transaction, ViewTab, AppSettings } from './types';
import { DEFAULT_CATEGORIES, DEFAULT_SETTINGS, INITIAL_BUDGET, INITIAL_TRANSACTIONS } from './data/initialData';
import { SovereignSidebar } from './components/SovereignSidebar';
import { SovereignTopbar } from './components/SovereignTopbar';
import { SovereignDashboard } from './components/SovereignDashboard';
import { GlobalGradientDefs } from './components/GradientIcon';
import { TransactionList } from './components/TransactionList';
import { BudgetOverview } from './components/BudgetOverview';
import { FinancialIntelligence } from './components/FinancialIntelligence';
import { AnalyticsCharts } from './components/AnalyticsCharts';
import { HistoricalAnalysis } from './components/HistoricalAnalysis';
import { FinancialGoals } from './components/FinancialGoals';
import { CardsAndInstallments } from './components/CardsAndInstallments';
import { Subscriptions } from './components/Subscriptions';
import { CategoryManager } from './components/CategoryManager';
import { Patrimony } from './components/Patrimony';
import { FutureCommitments } from './components/FutureCommitments';
import { MoneyAI } from './components/MoneyAI';
import { TransactionFormModal } from './components/TransactionFormModal';
import { ExportImportModal } from './components/ExportImportModal';
import { SafeImportModal } from './components/SafeImportModal';
import { SupabaseSyncModal } from './components/SupabaseSyncModal';
import { getCurrentMonthKey } from './utils/formatters';
import { categorizeTransaction } from './utils/autoCategorization';
import { supabase } from './lib/supabase';
import { readUserDataFromSupabase } from './lib/supabaseRead';
import { subscribeToCalculatorRealtime, unsubscribeFromCalculatorRealtime } from './lib/supabaseRealtime';
import { deleteCardFromSupabase, syncCardToSupabase, syncInstallmentPlanToSupabase } from './lib/supabaseCards';
import { deleteCategoryFromSupabase, deletePatrimonyItemFromSupabase, deleteTransactionFromSupabase, syncBudgetToSupabase, syncCategoryToSupabase, syncNetWorthSnapshotToSupabase, syncPatrimonyItemToSupabase, syncSettingsToSupabase, syncTransactionToSupabase } from './lib/supabaseWrite';

const STORAGE_KEYS={TRANSACTIONS:'mis_gastos_transactions_v1',CATEGORIES:'mis_gastos_categories_v1',BUDGETS:'mis_gastos_budgets_v1',SETTINGS:'mis_gastos_settings_v1',CARDS:'cream_financial_cards_v1',INSTALLMENTS:'cream_installment_plans_v1',SUBSCRIPTIONS:'cream_subscriptions_v1',PATRIMONY:'cream_patrimony_v1',NET_WORTH:'cream_net_worth_v1',COMMITMENTS:'cream_future_commitments_v1'};
const createTransactionId=()=>typeof crypto!=='undefined'&&typeof crypto['randomUUID']==='function'?`tx-${crypto['randomUUID']()}`:`tx-${Date.now()}-${Math.random().toString(36).slice(2,10)}`;
const createId=(prefix:string)=>typeof crypto!=='undefined'&&typeof crypto['randomUUID']==='function'?`${prefix}-${crypto['randomUUID']()}`:`${prefix}-${Date.now()}-${Math.random().toString(36).slice(2,10)}`;
const syncInBackground=(operation:()=>Promise<void>,label:string)=>{void operation().catch(error=>console.warn(`No se pudo sincronizar ${label} con Supabase. La copia local se mantiene.`,error));};
type CloudStatus='offline'|'connected'|'syncing'|'error';

export default function App(){
 const[currentMonthKey,setCurrentMonthKey]=useState<string>(getCurrentMonthKey());const[activeTab,setActiveTab]=useState<ViewTab>('dashboard');
 const[transactions,setTransactions]=useState<Transaction[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.TRANSACTIONS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return INITIAL_TRANSACTIONS});
 const[categories,setCategories]=useState<Category[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.CATEGORIES);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return DEFAULT_CATEGORIES});
 const[budgets,setBudgets]=useState<MonthlyBudget[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.BUDGETS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return INITIAL_BUDGET.monthKey?[INITIAL_BUDGET]:[]});
 const[settings,setSettings]=useState<AppSettings>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.SETTINGS);if(saved)return{...DEFAULT_SETTINGS,...JSON.parse(saved)}}catch(e){console.error(e)}return DEFAULT_SETTINGS});
 const[cards,setCards]=useState<FinancialCard[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.CARDS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[installmentPlans,setInstallmentPlans]=useState<InstallmentPlan[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.INSTALLMENTS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[subscriptions,setSubscriptions]=useState<Subscription[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.SUBSCRIPTIONS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[patrimonyItems,setPatrimonyItems]=useState<PatrimonyItem[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.PATRIMONY);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[netWorthSnapshots,setNetWorthSnapshots]=useState<NetWorthSnapshot[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.NET_WORTH);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[futureCommitments,setFutureCommitments]=useState<FutureCommitment[]>(()=>{try{const saved=localStorage.getItem(STORAGE_KEYS.COMMITMENTS);if(saved)return JSON.parse(saved)}catch(e){console.error(e)}return[]});
 const[isFormModalOpen,setIsFormModalOpen]=useState(false),[editingTransaction,setEditingTransaction]=useState<Transaction|null>(null),[isExportImportOpen,setIsExportImportOpen]=useState(false),[isSafeImportOpen,setIsSafeImportOpen]=useState(false),[isSupabaseSyncOpen,setIsSupabaseSyncOpen]=useState(false);
 const[hideValues,setHideValues]=useState(false),[isMobileMenuOpen,setIsMobileMenuOpen]=useState(false),[cloudStatus,setCloudStatus]=useState<CloudStatus>('offline'),[userEmail,setUserEmail]=useState('Sovereign Member');
 useEffect(()=>{localStorage.setItem(STORAGE_KEYS.TRANSACTIONS,JSON.stringify(transactions))},[transactions]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.CATEGORIES,JSON.stringify(categories))},[categories]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.BUDGETS,JSON.stringify(budgets))},[budgets]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.SETTINGS,JSON.stringify(settings))},[settings]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.CARDS,JSON.stringify(cards))},[cards]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.INSTALLMENTS,JSON.stringify(installmentPlans))},[installmentPlans]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.SUBSCRIPTIONS,JSON.stringify(subscriptions))},[subscriptions]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.PATRIMONY,JSON.stringify(patrimonyItems))},[patrimonyItems]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.NET_WORTH,JSON.stringify(netWorthSnapshots))},[netWorthSnapshots]);useEffect(()=>{localStorage.setItem(STORAGE_KEYS.COMMITMENTS,JSON.stringify(futureCommitments))},[futureCommitments]);
 useEffect(()=>{const root=document.documentElement;const applyTheme=()=>{const isLight=settings.theme==='light'||(settings.theme==='system'&&window.matchMedia('(prefers-color-scheme: light)').matches);root.classList.toggle('theme-light',isLight);root.classList.toggle('theme-dark',!isLight);root.style.colorScheme=isLight?'light':'dark'};applyTheme();if(settings.theme!=='system')return;const media=window.matchMedia('(prefers-color-scheme: light)');const handleChange=()=>applyTheme();media.addEventListener?.('change',handleChange);return()=>media.removeEventListener?.('change',handleChange)},[settings.theme]);
 useEffect(()=>{try{window.screen?.orientation?.unlock?.()}catch{}},[]);
 useEffect(()=>{const action=new URLSearchParams(window.location.search).get('quickAction');if(action!=='expense'&&action!=='income')return;window.history.replaceState({},document.title,window.location.pathname);const openTimer=window.setTimeout(()=>{const button=document.getElementById('topbar-new-tx-btn');button?.click();window.setTimeout(()=>{const wanted=action==='expense'?'Egreso / Desembolso':'Ingreso / Cobro';const typeButton=Array.from(document.querySelectorAll('button')).find(el=>el.textContent?.trim()===wanted) as HTMLButtonElement|undefined;typeButton?.click()},180)},250);return()=>window.clearTimeout(openTimer)},[]);
 useEffect(()=>{if(!supabase)return;let mounted=true;const update=async()=>{const{data:{session}}=await supabase.auth.getSession();if(mounted){setCloudStatus(session?.user?'connected':'offline');if(session?.user?.email)setUserEmail(session.user.email)}};void update();const{data:authListener}=supabase.auth.onAuthStateChange((_event,session)=>{if(mounted){setCloudStatus(session?.user?'connected':'offline');if(session?.user?.email)setUserEmail(session.user.email)}});const handle=(event:Event)=>{const detail=(event as CustomEvent<{status?:string}>).detail;if(detail?.status==='pending')setCloudStatus('syncing');if(detail?.status==='synced')setCloudStatus('connected');if(detail?.status==='error')setCloudStatus('error')};window.addEventListener('supabase-sync-status',handle);return()=>{mounted=false;authListener.subscription.unsubscribe();window.removeEventListener('supabase-sync-status',handle)}},[]);
 useEffect(()=>{if(!supabase)return;let cancelled=false,loadingUserId:string|null=null;let realtimeChannel:ReturnType<typeof subscribeToCalculatorRealtime>=null;const load=async(userId:string)=>{if(cancelled||loadingUserId===userId)return;loadingUserId=userId;try{const cloudData=await readUserDataFromSupabase(userId);if(cancelled)return;setTransactions(cloudData.transactions);setCategories(cloudData.categories);setBudgets(cloudData.budgets);if(cloudData.settings)setSettings(prev=>({...prev,...cloudData.settings}));setPatrimonyItems(cloudData.patrimonyItems);setNetWorthSnapshots(cloudData.netWorthSnapshots);setCards(cloudData.cards);setInstallmentPlans(cloudData.installmentPlans)}catch(error){console.warn('No se pudieron cargar los datos desde Supabase. Se mantiene la copia local.',error)}finally{if(loadingUserId===userId)loadingUserId=null}};const start=(userId:string)=>{if(cancelled||realtimeChannel)return;realtimeChannel=subscribeToCalculatorRealtime(userId,()=>{void load(userId)})};const current=async()=>{const{data:{session}}=await supabase.auth.getSession();if(!cancelled&&session?.user){await load(session.user.id);start(session.user.id)}};void current();const{data:authListener}=supabase.auth.onAuthStateChange((event,session)=>{if(cancelled)return;if(session?.user&&(event==='SIGNED_IN'||event==='INITIAL_SESSION'||event==='TOKEN_REFRESHED'))void load(session.user.id).then(()=>start(session.user.id));if(event==='SIGNED_OUT'&&realtimeChannel){void unsubscribeFromCalculatorRealtime(realtimeChannel);realtimeChannel=null}});return()=>{cancelled=true;authListener.subscription.unsubscribe();if(realtimeChannel)void unsubscribeFromCalculatorRealtime(realtimeChannel)}},[]);
 const monthTransactions=transactions.filter(t=>t.date.startsWith(currentMonthKey));const currentBudget=budgets.find(b=>b.monthKey===currentMonthKey)||{monthKey:currentMonthKey,totalTarget:0,categoryTargets:{}};const totalAssets=patrimonyItems.filter(i=>i.type==='asset').reduce((s,i)=>s+i.value,0);const totalLiabilities=patrimonyItems.filter(i=>i.type==='liability').reduce((s,i)=>s+i.value,0);const netWorthTotal=totalAssets-totalLiabilities;
 const handleSaveTransaction=(txData:Omit<Transaction,'id'|'createdAt'>)=>{if(editingTransaction){const updated={...editingTransaction,...txData};setTransactions(prev=>prev.map(t=>t.id===editingTransaction.id?updated:t));syncInBackground(()=>syncTransactionToSupabase(updated),'el movimiento');setEditingTransaction(null)}else{const planId=txData.installmentTotal&&txData.installmentTotal>1&&txData.cardId?createId('installment'):undefined;const newTx:Transaction={...txData,id:createTransactionId(),installmentPlanId:planId,createdAt:new Date().toISOString()};const defaultCategoryId=categories.find(c=>c.type===newTx.type||c.type==='both')?.id;const categorized=newTx.categoryId===defaultCategoryId?categorizeTransaction(newTx,categories,transactions):newTx;const finalTx=planId?{...categorized,installmentPlanId:planId}:categorized;setTransactions(prev=>[finalTx,...prev]);syncInBackground(()=>syncTransactionToSupabase(finalTx),'el movimiento');if(planId&&finalTx.cardId&&finalTx.installmentTotal&&finalTx.installmentTotalAmount){const plan:InstallmentPlan={id:planId,cardId:finalTx.cardId,title:finalTx.title,totalAmount:finalTx.installmentTotalAmount,installmentAmount:finalTx.amount,installments:finalTx.installmentTotal,currentInstallment:1,startDate:finalTx.date,transactionId:finalTx.id,notes:finalTx.notes,createdAt:finalTx.createdAt};setInstallmentPlans(prev=>[plan,...prev]);syncInBackground(()=>syncInstallmentPlanToSupabase(plan),'el plan de cuotas')}}};
 const handleDuplicateTransaction=(tx:Transaction)=>{const duplicated={...tx,id:createTransactionId(),title:`${tx.title} (Copia)`,createdAt:new Date().toISOString()};setTransactions(prev=>[duplicated,...prev]);syncInBackground(()=>syncTransactionToSupabase(duplicated),'el movimiento duplicado')};const handleDeleteTransaction=(id:string)=>{if(window.confirm('¿Seguro que deseas borrar este registro?')){setTransactions(prev=>prev.filter(t=>t.id!==id));syncInBackground(()=>deleteTransactionFromSupabase(id),'la eliminación del movimiento')}};
 const handleUpdateBudget=(newBudget:MonthlyBudget)=>{setBudgets(prev=>prev.some(b=>b.monthKey===newBudget.monthKey)?prev.map(b=>b.monthKey===newBudget.monthKey?newBudget:b):[...prev,newBudget]);syncInBackground(()=>syncBudgetToSupabase(newBudget),'el presupuesto')};const handleAddCategory=(newCat:Omit<Category,'id'>)=>{const created={...newCat,id:`cat-${Date.now()}`};setCategories(prev=>[...prev,created]);syncInBackground(()=>syncCategoryToSupabase(created),'la categoría')};const handleUpdateCategory=(updated:Category)=>{setCategories(prev=>prev.map(c=>c.id===updated.id?updated:c));syncInBackground(()=>syncCategoryToSupabase(updated),'la categoría')};const handleDeleteCategory=(id:string)=>{if(window.confirm('¿Deseas eliminar esta categoría?')){setCategories(prev=>prev.filter(c=>c.id!==id));syncInBackground(()=>deleteCategoryFromSupabase(id),'la eliminación de la categoría')}};
 const handleAddCard=(newCard:Omit<FinancialCard,'id'|'createdAt'>)=>{const card={...newCard,id:createId('card'),createdAt:new Date().toISOString()};setCards(prev=>[card,...prev]);syncInBackground(()=>syncCardToSupabase(card),'la tarjeta')};const handleUpdateCard=(updated:FinancialCard)=>{setCards(prev=>prev.map(c=>c.id===updated.id?updated:c));syncInBackground(()=>syncCardToSupabase(updated),'la tarjeta')};const handleDeleteCard=(id:string)=>{if(window.confirm('¿Eliminar la tarjeta? Los movimientos existentes no se borrarán.')){setCards(prev=>prev.filter(c=>c.id!==id));syncInBackground(()=>deleteCardFromSupabase(id),'la eliminación de la tarjeta')}};
 const handleAddSubscription=(data:Omit<Subscription,'id'|'createdAt'|'updatedAt'>)=>{const now=new Date().toISOString();setSubscriptions(prev=>[{...data,id:createId('subscription'),createdAt:now,updatedAt:now},...prev])};const handleUpdateSubscription=(updated:Subscription)=>setSubscriptions(prev=>prev.map(s=>s.id===updated.id?updated:s));const handleDeleteSubscription=(id:string)=>setSubscriptions(prev=>prev.filter(s=>s.id!==id));
 const handleAddPatrimony=(data:Omit<PatrimonyItem,'id'|'createdAt'|'updatedAt'>)=>{const now=new Date().toISOString();const item={...data,id:createId('asset'),createdAt:now,updatedAt:now};setPatrimonyItems(prev=>[item,...prev]);syncInBackground(()=>syncPatrimonyItemToSupabase(item),'el patrimonio')};const handleUpdatePatrimony=(item:PatrimonyItem)=>{const updated={...item,updatedAt:new Date().toISOString()};setPatrimonyItems(prev=>prev.map(i=>i.id===updated.id?updated:i));syncInBackground(()=>syncPatrimonyItemToSupabase(updated),'el patrimonio')};const handleDeletePatrimony=(id:string)=>{if(window.confirm('¿Eliminar este registro patrimonial?')){setPatrimonyItems(prev=>prev.filter(i=>i.id!==id));syncInBackground(()=>deletePatrimonyItemFromSupabase(id),'la eliminación del patrimonio')}};
 useEffect(()=>{if(!patrimonyItems.length)return;const assets=patrimonyItems.filter(i=>i.type==='asset').reduce((s,i)=>s+i.value,0),liabs=patrimonyItems.filter(i=>i.type==='liability').reduce((s,i)=>s+i.value,0),snapshotDate=new Date().toISOString().slice(0,10);const snapshot:NetWorthSnapshot={id:createId('snapshot'),snapshotDate,totalAssets:assets,totalLiabilities:liabs,netWorth:assets-liabs,createdAt:new Date().toISOString()};setNetWorthSnapshots(prev=>{const existing=prev.find(s=>s.snapshotDate===snapshotDate);return existing?prev.map(s=>s.snapshotDate===snapshotDate?{...s,totalAssets:assets,totalLiabilities:liabs,netWorth:assets-liabs}:s):[snapshot,...prev]});syncInBackground(()=>syncNetWorthSnapshotToSupabase(snapshot),'el historial patrimonial')},[patrimonyItems]);
 const handleAddCommitment=(data:Omit<FutureCommitment,'id'|'createdAt'|'updatedAt'>)=>{const now=new Date().toISOString();setFutureCommitments(prev=>[{...data,id:createId('commitment'),createdAt:now,updatedAt:now},...prev])};const handleUpdateCommitment=(item:FutureCommitment)=>setFutureCommitments(prev=>prev.map(c=>c.id===item.id?{...item,updatedAt:new Date().toISOString()}:c));const handleDeleteCommitment=(id:string)=>{if(window.confirm('¿Eliminar este compromiso futuro?'))setFutureCommitments(prev=>prev.filter(c=>c.id!==id))};const handleThemeChange=(theme:AppSettings['theme'])=>{const next={...settings,theme};setSettings(next);syncInBackground(()=>syncSettingsToSupabase(next),'el tema visual')};
 const handleImportFullData=(imported:{transactions:Transaction[];categories:Category[];budgets:MonthlyBudget[];settings:AppSettings})=>{setTransactions(imported.transactions);setCategories(imported.categories);setBudgets(imported.budgets);setSettings({...DEFAULT_SETTINGS,...imported.settings})};const handleSafeImportTransactions=(imported:Transaction[])=>{setTransactions(prev=>[...imported,...prev]);imported.forEach(tx=>syncInBackground(()=>syncTransactionToSupabase(tx),'el movimiento importado'))};const handleResetSampleData=()=>{setTransactions(INITIAL_TRANSACTIONS);setCategories(DEFAULT_CATEGORIES);setBudgets(INITIAL_BUDGET.monthKey?[INITIAL_BUDGET]:[]);setSettings(DEFAULT_SETTINGS)};
 return <div className="min-h-screen bg-[var(--cream-bg)] text-[var(--cream-text)] font-sans flex flex-row selection:bg-emerald-500 selection:text-zinc-950 transition-colors duration-150"><GlobalGradientDefs/><SovereignSidebar activeTab={activeTab} onTabChange={setActiveTab} cloudStatus={cloudStatus} onOpenSupabaseSync={()=>setIsSupabaseSyncOpen(true)} userEmail={userEmail} isMobileOpen={isMobileMenuOpen} onCloseMobile={()=>setIsMobileMenuOpen(false)}/><div className="flex-1 flex flex-col min-w-0"><SovereignTopbar currentMonthKey={currentMonthKey} onMonthChange={setCurrentMonthKey} onOpenNewTransaction={()=>{setEditingTransaction(null);setIsFormModalOpen(true)}} onOpenExportImport={()=>setIsExportImportOpen(true)} onOpenSupabaseSync={()=>setIsSupabaseSyncOpen(true)} currencySymbol={settings.currencySymbol} theme={settings.theme} onThemeChange={handleThemeChange} netWorthTotal={netWorthTotal} hideValues={hideValues} onToggleHideValues={()=>setHideValues(prev=>!prev)} onOpenMobileMenu={()=>setIsMobileMenuOpen(true)}/><main className="flex-1 max-w-7xl w-full mx-auto px-3 sm:px-6 lg:px-8 py-6">{activeTab==='dashboard'&&<SovereignDashboard currentMonthKey={currentMonthKey} transactions={transactions} categories={categories} budget={currentBudget} cards={cards} commitments={futureCommitments} subscriptions={subscriptions} installmentPlans={installmentPlans} patrimonyItems={patrimonyItems} snapshots={netWorthSnapshots} currencySymbol={settings.currencySymbol} hideValues={hideValues} onNavigateTab={setActiveTab} onOpenNewTransaction={()=>{setEditingTransaction(null);setIsFormModalOpen(true)}}/>}{activeTab==='transactions'&&<TransactionList transactions={transactions} categories={categories} currencySymbol={settings.currencySymbol} currentMonthKey={currentMonthKey} onEdit={tx=>{setEditingTransaction(tx);setIsFormModalOpen(true)}} onDuplicate={handleDuplicateTransaction} onDelete={handleDeleteTransaction} onAddNew={()=>{setEditingTransaction(null);setIsFormModalOpen(true)}}/>}{activeTab==='budgets'&&<div className="space-y-6"><FinancialIntelligence currentMonthKey={currentMonthKey} budgetTarget={currentBudget.totalTarget} transactions={monthTransactions} currencySymbol={settings.currencySymbol}/><BudgetOverview currentMonthKey={currentMonthKey} monthlyBudget={currentBudget} transactions={monthTransactions} categories={categories} currencySymbol={settings.currencySymbol} onUpdateBudget={handleUpdateBudget}/></div>}{activeTab==='analytics'&&<div className="space-y-6"><AnalyticsCharts transactions={monthTransactions} categories={categories} currencySymbol={settings.currencySymbol}/><HistoricalAnalysis transactions={transactions} currencySymbol={settings.currencySymbol}/><FinancialGoals transactions={transactions} currencySymbol={settings.currencySymbol}/></div>}{activeTab==='money-ai'&&<MoneyAI currentMonthKey={currentMonthKey} transactions={transactions} categories={categories} budget={currentBudget} commitments={futureCommitments} installmentPlans={installmentPlans} subscriptions={subscriptions} patrimonyItems={patrimonyItems} cards={cards} currencySymbol={settings.currencySymbol}/>} {activeTab==='patrimony'&&<Patrimony items={patrimonyItems} snapshots={netWorthSnapshots} currencySymbol={settings.currencySymbol} onAdd={handleAddPatrimony} onUpdate={handleUpdatePatrimony} onDelete={handleDeletePatrimony}/>} {activeTab==='commitments'&&<FutureCommitments commitments={futureCommitments} installmentPlans={installmentPlans} subscriptions={subscriptions} transactions={transactions} categories={categories} cards={cards} currencySymbol={settings.currencySymbol} onAdd={handleAddCommitment} onUpdate={handleUpdateCommitment} onDelete={handleDeleteCommitment}/>} {activeTab==='cards'&&<CardsAndInstallments cards={cards} installmentPlans={installmentPlans} transactions={transactions} currentMonthKey={currentMonthKey} currencySymbol={settings.currencySymbol} onAddCard={handleAddCard} onUpdateCard={handleUpdateCard} onDeleteCard={handleDeleteCard}/>} {activeTab==='subscriptions'&&<Subscriptions subscriptions={subscriptions} categories={categories} cards={cards} transactions={transactions} currentMonthKey={currentMonthKey} currencySymbol={settings.currencySymbol} onAdd={handleAddSubscription} onUpdate={handleUpdateSubscription} onDelete={handleDeleteSubscription}/>} {activeTab==='categories'&&<CategoryManager categories={categories} onAddCategory={handleAddCategory} onUpdateCategory={handleUpdateCategory} onDeleteCategory={handleDeleteCategory}/>}</main></div><TransactionFormModal isOpen={isFormModalOpen} onClose={()=>{setIsFormModalOpen(false);setEditingTransaction(null)}} onSave={handleSaveTransaction} editingTransaction={editingTransaction} categories={categories} cards={cards} currentMonthKey={currentMonthKey} currencySymbol={settings.currencySymbol}/><ExportImportModal isOpen={isExportImportOpen} onClose={()=>setIsExportImportOpen(false)} transactions={transactions} categories={categories} budgets={budgets} settings={settings} onImportFullData={handleImportFullData} onResetSampleData={handleResetSampleData} onOpenSafeImport={()=>{setIsExportImportOpen(false);setIsSafeImportOpen(true)}}/><SafeImportModal isOpen={isSafeImportOpen} onClose={()=>setIsSafeImportOpen(false)} transactions={transactions} categories={categories} onImportTransactions={handleSafeImportTransactions}/><SupabaseSyncModal isOpen={isSupabaseSyncOpen} onClose={()=>setIsSupabaseSyncOpen(false)} transactions={transactions} categories={categories} budgets={budgets} settings={settings}/></div>;
}
