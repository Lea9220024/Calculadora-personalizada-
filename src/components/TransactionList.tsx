import React, { useMemo, useState } from 'react';
import { Search, Edit2, Copy, Calendar, Repeat, Receipt, Plus, Ban } from 'lucide-react';
import { Category, PaymentMethod, Transaction } from '../types';
import { CategoryIcon } from './CategoryIcon';
import { formatCurrency, formatDateSpanish, PAYMENT_METHOD_LABELS } from '../utils/formatters';

interface TransactionListProps { transactions: Transaction[]; categories: Category[]; currencySymbol: string; currentMonthKey?: string; onEdit: (tx: Transaction) => void; onDuplicate: (tx: Transaction) => void; onDelete: (id: string) => void; onAddNew: () => void; }
const monthLabel = (key: string) => { const [year, month] = key.split('-').map(Number); if (!year || !month) return key; return new Intl.DateTimeFormat('es-AR', { month: 'long', year: 'numeric' }).format(new Date(year, month - 1, 1)); };

export const TransactionList: React.FC<TransactionListProps> = ({ transactions, categories, currencySymbol, currentMonthKey, onEdit, onDuplicate, onDelete, onAddNew }) => {
  const [searchTerm, setSearchTerm] = useState('');
  const [selectedMonth, setSelectedMonth] = useState(currentMonthKey || transactions[0]?.date?.slice(0, 7) || 'all');
  const [selectedCategory, setSelectedCategory] = useState('all');
  const [selectedMethod, setSelectedMethod] = useState('all');
  const [selectedType, setSelectedType] = useState('all');
  const months = useMemo(() => Array.from(new Set(transactions.map(t => t.date.slice(0, 7)).filter(Boolean))).sort((a, b) => b.localeCompare(a)), [transactions]);
  const getCategory = (catId: string) => categories.find(c => c.id === catId) || { id: 'desconocida', name: 'Sin Categoría', icon: 'Tag', color: 'bg-slate-400', textColor: 'text-slate-600', type: 'expense' as const };
  const filtered = useMemo(() => transactions.filter(tx => { if (selectedMonth !== 'all' && !tx.date.startsWith(selectedMonth)) return false; if (searchTerm.trim()) { const q = searchTerm.toLowerCase(); if (!tx.title.toLowerCase().includes(q) && !(tx.notes || '').toLowerCase().includes(q) && !getCategory(tx.categoryId).name.toLowerCase().includes(q)) return false; } if (selectedCategory !== 'all' && tx.categoryId !== selectedCategory) return false; if (selectedMethod !== 'all' && tx.paymentMethod !== selectedMethod) return false; if (selectedType !== 'all' && tx.type !== selectedType) return false; return true; }), [transactions, selectedMonth, searchTerm, selectedCategory, selectedMethod, selectedType, categories]);
  const sorted = [...filtered].sort((a, b) => b.date.localeCompare(a.date));
  const groupedByDate: Record<string, Transaction[]> = {}; sorted.forEach(tx => { (groupedByDate[tx.date] ||= []).push(tx); });
  const totalFilteredAmount = filtered.reduce((acc, tx) => tx.type === 'expense' ? acc - tx.amount : acc + tx.amount, 0);
  const clearFilters = () => { setSearchTerm(''); setSelectedMonth('all'); setSelectedCategory('all'); setSelectedMethod('all'); setSelectedType('all'); };
  return <div className="space-y-6 text-zinc-100">
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
      <div className="bg-[#171B26] border border-zinc-800 rounded-2xl p-4 shadow-sm"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-wider text-[#9AA6A0]">INGRESOS TOTALES</span><span className="text-[10px] px-2 py-0.5 rounded font-mono font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">{filtered.filter(t=>t.type==='income').length} operaciones</span></div><div className="text-2xl sm:text-3xl font-extrabold text-emerald-400 font-mono mt-2">+{formatCurrency(filtered.filter(t=>t.type==='income').reduce((s,t)=>s+t.amount,0),currencySymbol)}</div></div>
      <div className="bg-[#171B26] border border-zinc-800 rounded-2xl p-4 shadow-sm"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-wider text-[#9AA6A0]">GASTOS DEVENGADOS</span><span className="text-[10px] px-2 py-0.5 rounded font-mono font-bold bg-rose-500/10 text-rose-400 border border-rose-500/20">{filtered.filter(t=>t.type==='expense').length} operaciones</span></div><div className="text-2xl sm:text-3xl font-extrabold text-white font-mono mt-2">-{formatCurrency(filtered.filter(t=>t.type==='expense').reduce((s,t)=>s+t.amount,0),currencySymbol)}</div></div>
      <div className="bg-[#171B26] border border-zinc-800 rounded-2xl p-4 shadow-sm"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-wider text-[#9AA6A0]">FLUJO NETO SOBERANO</span><span className="text-[10px] px-2 py-0.5 rounded font-mono font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">Selección actual</span></div><div className={`text-2xl sm:text-3xl font-extrabold font-mono mt-2 ${totalFilteredAmount>=0?'text-emerald-400':'text-rose-400'}`}>{totalFilteredAmount>=0?'+':''}{formatCurrency(totalFilteredAmount,currencySymbol)}</div></div>
    </div>
    <div className="bg-[#171B26] rounded-2xl border border-zinc-800 shadow-md p-5 space-y-5">
      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div><div className="text-[10px] font-bold uppercase tracking-wider text-emerald-400">MÓDULO FINANCIERO · REGISTRO MULTICUENTA</div><h2 className="text-xl font-extrabold text-white flex items-center gap-2 mt-0.5 font-['Plus_Jakarta_Sans']">Movimientos <span className="text-xs font-semibold px-2.5 py-0.5 rounded-full bg-zinc-800 text-zinc-300 border border-zinc-700">{filtered.length} registrados</span></h2></div><button onClick={onAddNew} className="flex items-center justify-center gap-1.5 px-4 py-2 bg-emerald-500 hover:bg-emerald-400 text-black font-extrabold text-xs rounded-xl shadow-md" id="list-add-new-btn"><Plus className="w-4 h-4" strokeWidth={3}/><span>Registrar Movimiento</span></button></div>
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-5 gap-2.5">
        <div className="relative"><Search className="w-4 h-4 absolute left-3 top-1/2 -translate-y-1/2 text-zinc-400"/><input type="text" placeholder="Buscar por comercio, concepto, nota..." value={searchTerm} onChange={e=>setSearchTerm(e.target.value)} className="w-full pl-9 pr-3 py-2 bg-[#1C1F2A] border border-zinc-800 rounded-xl text-xs text-zinc-100 placeholder-[#9AA6A0] focus:outline-none focus:border-emerald-500"/></div>
        <select value={selectedMonth} onChange={e=>setSelectedMonth(e.target.value)} className="px-3 py-2 bg-[#1C1F2A] border border-zinc-800 rounded-xl text-xs font-semibold text-zinc-200 focus:outline-none focus:border-emerald-500 cursor-pointer" id="filter-month"><option value="all">Todos los meses</option>{months.map(m=><option key={m} value={m}>{monthLabel(m)}</option>)}</select>
        <select value={selectedType} onChange={e=>setSelectedType(e.target.value)} className="px-3 py-2 bg-[#1C1F2A] border border-zinc-800 rounded-xl text-xs font-semibold text-zinc-200 focus:outline-none focus:border-emerald-500 cursor-pointer"><option value="all">Todos los Tipos</option><option value="expense">Solo Gastos</option><option value="income">Solo Ingresos</option></select>
        <select value={selectedCategory} onChange={e=>setSelectedCategory(e.target.value)} className="px-3 py-2 bg-[#1C1F2A] border border-zinc-800 rounded-xl text-xs font-semibold text-zinc-200 focus:outline-none focus:border-emerald-500 cursor-pointer"><option value="all">Todas las Categorías</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select>
        <select value={selectedMethod} onChange={e=>setSelectedMethod(e.target.value)} className="px-3 py-2 bg-[#1C1F2A] border border-zinc-800 rounded-xl text-xs font-semibold text-zinc-200 focus:outline-none focus:border-emerald-500 cursor-pointer"><option value="all">Todos los Medios</option>{(Object.keys(PAYMENT_METHOD_LABELS) as PaymentMethod[]).map(m=><option key={m} value={m}>{PAYMENT_METHOD_LABELS[m]}</option>)}</select>
      </div>
      {(searchTerm||selectedMonth!=='all'||selectedCategory!=='all'||selectedMethod!=='all'||selectedType!=='all')&&<div className="flex items-center justify-between px-3 py-1.5 bg-[#1C1F2A] rounded-xl border border-zinc-800 text-xs"><span className="text-[#9AA6A0]">Filtros activos · Balance: <strong className={totalFilteredAmount>=0?'text-emerald-400 font-mono':'text-rose-400 font-mono'}>{formatCurrency(totalFilteredAmount,currencySymbol)}</strong></span><button onClick={clearFilters} className="text-xs text-emerald-400 hover:underline font-bold">Limpiar filtros</button></div>}
      {sorted.length===0?<div className="py-12 text-center border-2 border-dashed border-zinc-800 rounded-2xl p-6 bg-[#1C1F2A]/40"><Receipt className="w-6 h-6 text-emerald-400 mx-auto mb-3"/><h3 className="text-sm font-bold text-zinc-200">No hay movimientos registrados</h3><p className="text-xs text-[#9AA6A0] mt-1 mb-4">No encontramos movimientos que coincidan con tus filtros.</p><button onClick={onAddNew} className="inline-flex items-center gap-1.5 px-4 py-2 bg-emerald-500 text-black font-extrabold text-xs rounded-xl"><Plus className="w-4 h-4"/><span>Registrar Movimiento</span></button></div>:<div className="space-y-6">{Object.entries(groupedByDate).map(([dateStr,items])=>{const dayExpenseTotal=items.filter(i=>i.type==='expense').reduce((s,i)=>s+i.amount,0);const dayIncomeTotal=items.filter(i=>i.type==='income').reduce((s,i)=>s+i.amount,0);return <div key={dateStr} className="space-y-2"><div className="flex items-center justify-between px-1 border-b border-zinc-800/80 pb-1.5"><div className="flex items-center gap-2"><Calendar className="w-3.5 h-3.5 text-emerald-400"/><span className="text-xs font-bold text-zinc-200">{formatDateSpanish(dateStr)}</span></div><div className="flex items-center gap-3 text-xs font-mono">{dayIncomeTotal>0&&<span className="text-emerald-400 font-extrabold">+{formatCurrency(dayIncomeTotal,currencySymbol)}</span>}{dayExpenseTotal>0&&<span className="text-white font-extrabold">-{formatCurrency(dayExpenseTotal,currencySymbol)}</span>}</div></div><div className="divide-y divide-zinc-800/60 rounded-xl border border-zinc-800/80 bg-[#171B26] overflow-hidden shadow-sm">{items.map(tx=>{const cat=getCategory(tx.categoryId);return <div key={tx.id} className="p-3.5 sm:p-4 hover:bg-zinc-800/40 transition-colors flex items-center justify-between gap-3" id={`tx-item-${tx.id}`}><div className="flex items-center gap-3 min-w-0"><div className="w-10 h-10 rounded-xl bg-[#1C1F2A] border border-zinc-800 text-white flex items-center justify-center shrink-0 shadow-sm"><CategoryIcon name={cat.icon} className="w-5 h-5 text-emerald-400" strokeWidth={2.4}/></div><div className="min-w-0 space-y-0.5"><div className="flex items-center gap-2 flex-wrap"><span className="font-extrabold text-white text-sm truncate">{tx.title}</span>{tx.isRecurring&&<span className="inline-flex items-center gap-0.5 px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/30"><Repeat className="w-2.5 h-2.5"/>Recurrente</span>}</div><div className="flex items-center gap-2 text-xs text-[#9AA6A0] flex-wrap"><span className="font-semibold text-zinc-300">{cat.name}</span><span>•</span><span className="capitalize">{PAYMENT_METHOD_LABELS[tx.paymentMethod]||tx.paymentMethod}</span>{tx.notes&&<><span>•</span><span className="italic text-zinc-500 truncate max-w-[180px]">"{tx.notes}"</span></>}</div></div></div><div className="flex items-center gap-3 shrink-0"><div className="text-right"><div className={`font-black text-sm sm:text-base font-mono ${tx.type==='expense'?'text-white':'text-emerald-400'}`}>{tx.type==='expense'?'-':'+'}{formatCurrency(tx.amount,currencySymbol)}</div></div><div className="flex items-center gap-1 shrink-0" aria-label="Acciones del movimiento"><button onClick={()=>onEdit(tx)} className="p-2 text-zinc-300 hover:text-white hover:bg-zinc-800 rounded-lg transition-colors" title="Editar" aria-label="Editar movimiento"><Edit2 className="w-4 h-4"/></button><button onClick={()=>onDuplicate(tx)} className="p-2 text-zinc-300 hover:text-white hover:bg-zinc-800 rounded-lg transition-colors" title="Copiar" aria-label="Copiar movimiento"><Copy className="w-4 h-4"/></button><button onClick={()=>onDelete(tx.id)} className="p-2 text-zinc-300 hover:text-rose-400 hover:bg-zinc-800 rounded-lg transition-colors" title="Anular" aria-label="Anular movimiento"><Ban className="w-4 h-4"/></button></div></div></div>})}</div></div>})}</div>}
    </div>
  </div>;
};
